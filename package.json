{
  "name": "sunnydale-stake-night",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript build for the Stake Night canvas game (compiled into wwwroot/js).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
